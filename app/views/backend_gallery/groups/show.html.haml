%h1= _("Show group: %{title}", :title => @group.title)

= link_to edit_group_path(@group), :class => "btn" do
  %i.icon-pencil
  = _("Edit")

= link_to groups_path, :class => "btn" do
  %i.icon-arrow-left
  = _("Back")

= link_to new_group_picture_path(@group), :class => "btn" do
  %i.icon-pencil
  = _("New picture")
